nearly everything comes off the games end point and required a game_id, things that
don't require the games endpoint are
    - Users
    - Reports
    - Me

class Bot:
    api key

class Game:
    id -> int
    game_id -> int
    UNIX TIMESTAPMPS
        date_added
        date_updated
        date_live
    logo -> dict with links to the various resolutions & original 
        # include file name?

    homepage -> url or null (None)
    name -> str for non-unique game name
    name_id -> str for unique game name
    summary -> str
    description -> str
    metadata_blob -> can be null #"Comma-separated list of metadata words."
    submitted_by -> User obj that submitted the mod

class User:

class Mod:

    